{
  "name": "storyforge-studio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "dev:frontend": "pnpm --filter desktop dev",
    "build:frontend": "pnpm --filter desktop build",
    "typecheck": "pnpm -r --if-present typecheck",
    "lint": "pnpm -r --if-present lint"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.3.1"
  }
}
